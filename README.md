# Terraform AWS Infrastructure — Basic EC2 + Security Group

## Files
- `main.tf` — Defines provider, security group, and EC2 instance.
- `variables.tf` — Defines input variables.
- `outputs.tf` — (Optional) Output definitions.

## Usage
```bash
terraform init
terraform plan
terraform apply
